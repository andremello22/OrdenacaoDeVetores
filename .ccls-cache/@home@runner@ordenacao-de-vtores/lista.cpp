#include <iostream>
#include <cstdlib>
using namespace std;
struct t_no{
int id;
struct t_no *prox;
}typedef no;
int inserir(no *cabeca, int id){
  no *aux = (no*)malloc(sizeof(no));
  no *temp = NULL;
  aux->id = id;
  aux->prox = NULL;
}
int main(){
cout<<".";

}